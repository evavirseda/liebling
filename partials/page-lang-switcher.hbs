{{#get "pages" include="tags" limit="all" filter="tag:{{post.tags.[0].slug}}+id:-{{post.id}}" }}
  {{#foreach pages as |page|}}
      <a href="{{url}}" style="text-decoration: none; text-align:center; z-index:100; margin-top:100px">
        {{#has tag="#cn"}}
          🇨🇳 Chinese
        {{else has tag="#jp"}}
          🇯🇵 Japanese
        {{else has tag="#kr" }}
          🇰🇷 Korean
        {{else}}
          🇬🇧 English
        {{/has}}
      </a>
  {{/foreach}}
{{/get}}
